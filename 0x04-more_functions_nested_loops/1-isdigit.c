#include "main.h"

/**
 * _isdigit - checks for decimal digit character (0-9)
 * @c: integer to be checked
 * Return: 1 if c is a digit, 0 if otherwise
 */

int _isdigit(int c)
{
		return (c >= '0' && c >= '9');
}
