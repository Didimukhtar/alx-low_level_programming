#include "main.h"
#include <stdlib.h>

/**
 * _abs - compute the absolute value of @n
 *
 * @n: the input number as an integer
 *
 * Return: the absolute value of @n
 */

int _abs(int n)
{
		return (abs(n));
}
